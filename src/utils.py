def init_dict(keys):
    """
    Init dictionary with countries and zero
    :param keys: keys to be put into dict
    :return: new dict
    """
    _dict = {}
    for key in keys:
        _dict[key] = 0

    return _dict